# 'if' [ decl ';' ] condition stmt_or_block [ 'else' stmt_or_block ]
if i int := 0.0; i > 0 {
    # if will not run
} else if j int := 1; j < 10 {
    # else if will run
} else {
    # else will not run
}
